Welcome to my KEHUD!

To edit some settings, go to modSettings.
Each modSetting corresponds to the script in Scripts of the same name.
Some modSettings are set inside the script, so be careful what you set!
	Don't worry though, there is a txt file for each json in case you're confused!